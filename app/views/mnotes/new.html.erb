<% if user_signed_in? %>
  <div class='wrapper'>
    <div class="row">
      <h1 class="form_header">Add a Misc Recruit Note</h1>
    </div>
    <%= form_for @mnote do |f| %>
      <div class="row">
        <%= f.label :recruit_id, 'Recruit', class: 'form-actions' %>
        <%= f.select :recruit_id, options_for_select(@recruits) %>
      </div>
      <div class="row">
        <%= f.label :text, 'Note', class: 'form-actions' %>
        <%= f.text_area :text, cols: "50", rows: "25" %>
      </div>
      <div class="form-actions">
        <%= f.submit 'Submit' %>
      </div>
    <% end %>
  </div>
<% end %>
