<div class="wrapper">
 <div class="index_header">
   <h3>Positions</h3>
 </div>
  <% @positions.each do |f| %>
    <a href= <%= edit_position_path(f.id) %> class='more_links' >
      <h4><%= f.name %></h4>
    </a>
  <% end %>
</div>
