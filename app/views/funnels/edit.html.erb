<%= form_for @funnel do |f| %>
  <% if @funnel.errors.any? %>
    <div>
      <h2><%= pluralize(@funnel.errors.count, "error") %> prohibited this note from being saved:</h2>
      <ul>
        <% @funnel.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class='wrapper'>
    <div class="row">
      <h1 class="form_header">Update Funnel</h1>
    </div>
    <div class="row">
      <%= f.label :name, 'Name', class: 'form-actions' %>
      <%= f.text_field :name, class: 'text_field' %>
    </div>
    <div class="form-actions">
      <%= f.button 'Update' %>
      <p><%= link_to "Delete Funnel", funnel_path(@funnel.id), data: { confirm: "Are you sure? Funnel item should only be deleted if it is a duplicate" }, method: :delete, :class => 'user_links' %> <br />
    </div>
  </div>
<% end %>
